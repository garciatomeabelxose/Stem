/*1. Al cargar la página:
   - Añade un nuevo integrante `"Trompetista"` a la lista.
   - Elimina `"Batería"` de la lista.*/

let lista = document.getElementById("integrantes");
let elementosLista = lista.querySelectorAll("li")
let nuevoIntegrante = "Trompetista"
nuevoIntegrante.textContent = nuevoIntegrante;

for (let i = 0; i < elementosLista.length; i++) {
    if (lista.textContent == "Bater")
    
}